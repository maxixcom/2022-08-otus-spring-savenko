spring:
  shell:
    interactive:
      enabled: false
  data:
    mongodb:
      port: 27018
      database: test

  mongodb:
    embedded:
      version: 5.0.0

mongock:
  enabled: false

logging:
  level:
    org:
      springframework:
        boot:
          autoconfigure:
            mongo:
              embedded:
                EmbeddedMongo: WARN
